#include <stdio.h>

int main() 
{
    int n, i,sum = 0;
    printf("Add your number to find even sum : ");
  scanf ("%d" , &n);
  
  for(i= 2 ; i<=n;i++){
      if(i%2==0){
       
            sum=sum+i;
      }
         }
  printf("sum of %d is = %d" ,n ,sum);
  
    return 0; 
}
